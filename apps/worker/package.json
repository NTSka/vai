{
  "name": "@vai/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --enable-source-maps dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @vai/backend build && vitest run",
    "lint": "echo \"worker lint is not configured yet\"",
    "format": "echo \"worker format is not configured yet\""
  },
  "dependencies": {
    "@vai/backend": "workspace:*",
    "@vai/domain-contracts": "workspace:*"
  },
  "devDependencies": {
    "@grpc/grpc-js": "^1.14.2",
    "@grpc/proto-loader": "^0.8.0",
    "vitest": "^3.2.4"
  }
}
